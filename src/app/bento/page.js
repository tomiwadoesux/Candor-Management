import BentoGrid from "../../components/BentoGrid";

// Scratch page for perfecting the board on its own, away from the landing page.
export const metadata = { title: "Bento" };

export default function Bento() {
  return <BentoGrid />;
}
